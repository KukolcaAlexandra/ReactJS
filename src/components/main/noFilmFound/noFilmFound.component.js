import React from 'react';
import styles from './noFilmFound.css';

const NoFilmFound = () => (
  <div className={styles.center}>
    <p className={styles.noFound}>No films found</p>
  </div>
);

export default NoFilmFound;
